<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background: url('/svit_pic.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .dashboard-overlay {
            background: rgba(255,255,255,0.96);
            border-radius: 20px;
            padding: 40px 35px 35px 35px;
            box-shadow: 0 8px 32px 0 rgba(31,38,135,0.18);
            max-width: 500px;
            width: 100%;
        }
        .logo-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 18px;
        }
        .college-logo {
            width: 65px;
            height: 65px;
            object-fit: contain;
            border-radius: 50%;
            box-shadow: 0 2px 8px rgba(44,62,80,0.10);
            background: #fff;
            padding: 6px;
        }
        .dashboard-title {
            font-weight: 700;
            letter-spacing: 2px;
            color: #2c3e50;
            text-align: center;
            margin-bottom: 18px;
        }
        .dashboard-actions {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-top: 25px;
        }
        .dashboard-actions .btn {
            font-weight: 600;
            border-radius: 8px;
            letter-spacing: 1px;
            font-size: 1.08rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .dashboard-actions .btn i {
            margin-right: 8px;
        }
        .search-form {
            margin-top: 18px;
        }
        .form-control {
            border-radius: 8px;
        }
    </style>
</head>
<body>
 <div class="dashboard-overlay mx-auto">
        <div class="logo-container">
            <img src="/logo.png" alt="College Logo" class="college-logo">
        </div>
        <h1 class="dashboard-title">
            <i class="fas fa-user-shield"></i><br>
            Admin Dashboard
        </h1>
        <form action="/search-student" method="post" class="search-form">
            <div class="form-group">
                <label for="roll"><i class="fas fa-search"></i> Search Student by Roll Number</label>
                <input type="text" class="form-control" id="roll" name="roll" required placeholder="Enter Roll Number">
            </div>
            <button type="submit" class="btn btn-primary w-100">
                <i class="fas fa-search"></i> Search
            </button>
        </form>
        <div class="dashboard-actions">
            <a href="/all-students" class="btn btn-success">
                <i class="fas fa-users"></i> Generate All Students
            </a>
            <a href="/logout" class="btn btn-secondary">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </div>
    </div>
</body>
</html>